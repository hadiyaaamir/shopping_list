import 'package:flutter/material.dart';

class AddUsersButton extends StatelessWidget {
  const AddUsersButton({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(Icons.person_add_alt_1),
      onPressed: () {},
    );
  }
}
